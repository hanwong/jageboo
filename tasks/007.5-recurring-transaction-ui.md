# Task 007.5: 반복 거래 UI 구현 ✅ 완료

## 개요

반복 거래 설정 및 관리를 위한 UI를 구현합니다. 매출/매입 입력 화면에 반복 거래 설정 섹션을 추가하고, 설정 페이지에 반복 거래 관리 UI를 구현합니다.

## 관련 기능

- **F007**: 반복 거래 등록
- **F013**: 반복 거래 관리 (활성화/비활성화)

## 구현 단계

### 1단계: 반복 거래 컴포넌트 생성

- [x] `components/recurring/recurring-form-section.tsx` 생성
  - 반복 거래 활성화 체크박스
  - 반복 주기 선택 (주간/월간) 셀렉트
  - 시작일 선택기
  - 종료일 선택기 (선택사항)

- [x] `components/recurring/recurring-transaction-card.tsx` 생성
  - 반복 거래 카드 UI
  - 금액, 메모, 주기 표시
  - 활성화/비활성화 토글 스위치

- [x] `components/recurring/recurring-transaction-list.tsx` 생성
  - 반복 거래 목록 컴포넌트
  - 빈 상태 UI

### 2단계: 더미 데이터 추가

- [x] `lib/constants/dummy-data.ts`에 `dummyRecurringTransactions` 추가
  - 월세, 인터넷/전화, 정기 배달 샘플 데이터

### 3단계: 설정 페이지 업데이트

- [x] `app/settings/page.tsx` 업데이트
  - RecurringTransactionList 컴포넌트 사용
  - 토글 핸들러 구현 (로컬 상태)
  - 더미 데이터로 UI 동작 확인

### 4단계: shadcn 컴포넌트 설치

- [x] Select 컴포넌트 설치
- [x] AlertDialog 컴포넌트 설치 (Task 006에서 완료)

## 수락 기준

- [x] 설정 페이지에 반복 거래 목록 표시됨
- [x] 활성화/비활성화 토글 정상 작동
- [x] 반복 주기, 금액, 메모가 정확히 표시됨
- [x] 빈 상태 UI 정상 표시
- [x] 모바일 화면에서 레이아웃 정상
- [x] `pnpm check-all` 통과
- [x] `pnpm build` 성공

## 변경 사항 요약

### 생성된 파일

- `components/recurring/recurring-form-section.tsx` - 반복 거래 설정 섹션
- `components/recurring/recurring-transaction-card.tsx` - 반복 거래 카드
- `components/recurring/recurring-transaction-list.tsx` - 반복 거래 목록
- `components/ui/select.tsx` - shadcn Select 컴포넌트

### 수정된 파일

- `lib/constants/dummy-data.ts` - 더미 반복 거래 데이터 추가
- `app/settings/page.tsx` - 반복 거래 관리 UI 구현

### 구현된 기능

- ✅ 반복 거래 목록 카드 표시
- ✅ 활성화/비활성화 토글 (로컬 상태)
- ✅ 반복 주기 표시 (주간/월간)
- ✅ 더미 데이터 기반 UI 동작

## 참고 사항

- Phase 2에서는 UI만 구현
- 실제 데이터 저장 및 자동 생성 로직은 Phase 3에서 구현 예정
- 매출/매입 입력 화면에 반복 거래 옵션 추가는 Phase 3에서 Server Action과 함께 구현 예정
